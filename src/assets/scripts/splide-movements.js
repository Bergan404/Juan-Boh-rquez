var splide = new Splide( '#sharing', {
    type: 'loop',
	perPage: 1,
	perMove: 1,
	arrows: false,
	autoplay: true,
	interval: 7000,
});

splide.mount();


var splide = new Splide( '#apparel', {
    type: 'loop',
	perPage: 1,
	perMove: 1,
	arrows: false,
	autoplay: true,
	interval: 7000,
});

splide.mount();

var splide = new Splide( '#web', {
    type: 'loop',
	perPage: 1,
	perMove: 1,
	arrows: false,
	autoplay: true,
	interval: 7000,
});

splide.mount();
